To develop locally, run:
`npm run dev`

To build, run:
`npm run build`


The project will use some CI/CD pipeline for deployment. Near the end of development, I will build and document the CI/CD process.
